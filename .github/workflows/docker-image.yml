# name: Docker Image CI
#
# on:
#   push:
#     branches: [ "main" ]
#
# jobs:
#
#   build:
#
#     runs-on: ubuntu-latest
#
#     steps:
#     - uses: actions/checkout@v4
#     - name: Build the Docker image
#       # run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
#       run: docker buildx build --platform linux/arm64/v8,linux/amd64 -t alexandrosm77/migraine_forecast:latest --push .

name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  docker:
    runs-on: ubuntu-latest
    environment: latest
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          platforms: linux/amd64,linux/arm64
          tags: alexandrosm77/migraine_forecast:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
#           cache-from: type=registry,ref=alexandrosm77/migraine_forecast:buildcache
#           cache-to: type=registry,ref=alexandrosm77/migraine_forecast:buildcache,mode=max

