name: Docker Hub CI (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: self-hosted
    steps:
      - name: Explain replacement workflow
        run: |
          echo "Docker Hub publishing is disabled."
          echo "Production deployments now use .github/workflows/compose-deploy.yml."
          echo "That workflow SSHes to the Raspberry Pi, builds locally, and runs Docker Compose."
